app.controller('homeCtrl', function ($state, companyService) {
    const self = this;
    
    const initController = () => {
       //TODO something
    };

    angular.element(document).ready(function () {
        initController();
    });
});